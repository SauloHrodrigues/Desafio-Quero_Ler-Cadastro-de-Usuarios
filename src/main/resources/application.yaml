spring:
  profiles:
    active: dev
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false
    clean-disabled: false
    #    clean-on-validation-error: true
    locations: classpath:db/migration